let current=0;const steps=[...document.querySelectorAll('.step')];const bar=document.querySelector('.progress-bar i');const count=document.querySelector('#stepCount');function show(n){current=Math.max(0,Math.min(n,steps.length-1));steps.forEach((s,i)=>s.classList.toggle('active',i===current));bar.style.width=((current+1)/steps.length*100)+'%';count.textContent=(current+1)+' / '+steps.length;window.scrollTo({top:0,behavior:'smooth'});}document.querySelectorAll('[data-next]').forEach(b=>b.addEventListener('click',()=>show(current+1)));document.querySelectorAll('[data-prev]').forEach(b=>b.addEventListener('click',()=>show(current-1)));document.querySelectorAll('.choice').forEach(c=>c.addEventListener('click',()=>{document.querySelectorAll('.choice').forEach(x=>x.classList.remove('selected'));c.classList.add('selected');document.querySelector('#choiceNext').disabled=false;}));const help=document.querySelector('.help'),hint=document.querySelector('.hint');help?.addEventListener('click',()=>hint.classList.toggle('show'));document.addEventListener('keydown',e=>{if(e.key==='Escape')hint.classList.remove('show')});