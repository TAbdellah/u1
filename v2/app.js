const teacherToggle=document.getElementById('teacherToggle');teacherToggle?.addEventListener('click',()=>{document.body.classList.toggle('teacher-mode');const on=document.body.classList.contains('teacher-mode');teacherToggle.innerHTML=on?'<i class="fa-solid fa-user-graduate"></i> Mode élève':'<i class="fa-solid fa-chalkboard-user"></i> Mode enseignant';if(on)document.getElementById('enseignant')?.scrollIntoView({behavior:'smooth'});});const links=[...document.querySelectorAll('.nav-link')];const sections=[...document.querySelectorAll('main section[id]')];const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+entry.target.id));}})},{rootMargin:'-35% 0px -55% 0px'});sections.forEach(s=>observer.observe(s));
